{
  "name": "ksiegowy-vibe",
  "private": true,
  "version": "2.0.1",
  "description": "Monorepo scaffold for ksiegowy-vibe.pl",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "sh -c 'set -a && . ./.env && set +a && pnpm -r --parallel --filter @ksiegowy/api --filter @ksiegowy/web dev'",
    "dev:web": "sh -c 'set -a && . ./.env && set +a && pnpm -r --parallel --filter @ksiegowy/web dev'",
    "build": "pnpm -r --if-present build",
    "typecheck": "pnpm --filter @ksiegowy/web typecheck && pnpm exec tsc --build tsconfig.json --pretty false",
    "lint": "pnpm -r --if-present lint",
    "test": "pnpm -r --if-present --filter '!@ksiegowy/e2e' test",
    "test:e2e": "pnpm --filter @ksiegowy/e2e test",
    "clean": "pnpm -r --if-present clean",
    "db:seed": "sh -c 'set -a && . ./.env && set +a && pnpm tsx scripts/seed.ts'",
    "ksef:seed-incoming": "sh -c 'set -a && . ./.env && set +a && pnpm tsx scripts/ksef-seed-incoming.ts \"$@\"' --",
    "db:migrate:company": "sh -c 'set -a && . ./.env && set +a && pnpm --filter @ksiegowy/api exec prisma migrate dev --schema prisma/schema.prisma'",
    "db:migrate:household": "sh -c 'set -a && . ./.env && set +a && pnpm --filter @ksiegowy/household-service exec prisma migrate dev --schema prisma/schema.prisma'",
    "db:migrate": "pnpm run db:migrate:company && pnpm run db:migrate:household",
    "db:studio:company": "sh -c 'set -a && . ./.env && set +a && pnpm --filter @ksiegowy/api exec prisma studio --schema prisma/schema.prisma'",
    "db:studio:household": "sh -c 'set -a && . ./.env && set +a && pnpm --filter @ksiegowy/household-service exec prisma studio --schema prisma/schema.prisma'",
    "db:studio": "pnpm run db:studio:company",
    "backup:postgres": "docker compose --profile backup run --rm -e DB_BACKUP_DATABASE_LABEL=all backup-postgres",
    "backup:postgres:company": "docker compose --profile backup run --rm -e DB_BACKUP_DATABASE_LABEL=business backup-postgres",
    "backup:postgres:household": "docker compose --profile backup run --rm -e DB_BACKUP_DATABASE_LABEL=household backup-postgres",
    "backup:postgres:local": "docker compose --profile backup run --rm -e DB_BACKUP_ENVIRONMENT_NAME=local -e DB_BACKUP_DATABASE_LABEL=all -e DB_BACKUP_REMOTE_PRIMARY_NAME= -e DB_BACKUP_REMOTE_SECONDARY_NAME= backup-postgres",
    "backup:postgres:local:company": "docker compose --profile backup run --rm -e DB_BACKUP_ENVIRONMENT_NAME=local -e DB_BACKUP_DATABASE_LABEL=business -e DB_BACKUP_REMOTE_PRIMARY_NAME= -e DB_BACKUP_REMOTE_SECONDARY_NAME= backup-postgres",
    "backup:postgres:local:household": "docker compose --profile backup run --rm -e DB_BACKUP_ENVIRONMENT_NAME=local -e DB_BACKUP_DATABASE_LABEL=household -e DB_BACKUP_REMOTE_PRIMARY_NAME= -e DB_BACKUP_REMOTE_SECONDARY_NAME= backup-postgres",
    "backup:postgres:download": "docker compose --profile backup run --rm download-postgres-backup",
    "restore:postgres": "docker compose --profile backup run --rm restore-postgres",
    "publish:container:images": "bash scripts/publish-container-images.sh",
    "test:publish:container:images": "bash scripts/publish-container-images.test.sh"
  },
  "pnpm": {
    "overrides": {
      "baseline-browser-mapping": "2.11.0",
      "browserslist": "4.29.0",
      "brace-expansion@1": "1.1.18",
      "brace-expansion@2": "2.1.4",
      "brace-expansion@5": "5.0.12",
      "deepmerge-ts": "8.0.2",
      "defu": "6.1.7",
      "effect": "3.22.2",
      "fast-uri@3": "3.1.8",
      "js-yaml": "4.3.2",
      "nanoid@3": "3.3.18",
      "piscina": "4.9.3",
      "postcss": "8.5.28",
      "vite": "7.3.5"
    }
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@next/eslint-plugin-next": "^16.2.2",
    "@types/node": "^22.14.1",
    "eslint": "^9.39.4",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^7.0.1",
    "tsx": "^4.20.5",
    "typescript": "^5.8.2",
    "typescript-eslint": "^8.58.0"
  }
}
